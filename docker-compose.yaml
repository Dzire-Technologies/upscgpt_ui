services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - BACKEND_URL=https://api.indianupscgpt.com/
    ports:
      - "4000:80"
    volumes:
      - .:/app